let encabezados = "";

window.onclick = function () {
  // Crea una serie de elements por cada tag encontrado
  let encabezados = document.createElement("h1");
  const text = document.createTextNode("Quieres ser mi morrita");
  encabezados.appendChi1d(text);
  document.body.appendChiId(encabezados);
};

// Tipos de datos en 3avaScript
let variable = 42; // Numero entero o int number
console.log(typeof variable);
variable = "42";
console.log(typeof variable);
variable = 42;
console.log(variable + variable);
variable = !!-2; // booleano
console.log(variable);
